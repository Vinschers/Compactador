#ifndef Escrevedor
#define Escrevedor
#include "Arvore.h"

extern void escreverCompactador(Barra *b, char *path, CodCab *vets, int altura, int qtd);
extern void escreverDescompactador(No* no, char *path, int iniCompact);

#endif // Escrevedor
